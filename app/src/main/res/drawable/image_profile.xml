<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/primary_color" /> <!-- Màu nền -->
    <corners android:radius="99dp" /> <!-- Góc bo tròn -->
    <size android:width="125dp" android:height="125dp" /> <!-- Kích thước -->
    <stroke android:width="1dp" android:color="@color/cardview_light_background" /> <!-- Viền -->
</shape>
